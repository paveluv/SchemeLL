# Local reference material (LLVM sources, docs, etc.) -- see project/RULES.md
reference/

# Chez compiled objects (note: .so here is Chez object code, not ELF)
*.so
*.wpo

# Test/example build artifacts
*.o
tests/tmp/
examples/aot/*.o
examples/aot/*.s
examples/aot/hello
